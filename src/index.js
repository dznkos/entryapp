import React from 'react';
import ReactDOM from 'react-dom';

import { EntryFree } from './EntryFree';

ReactDOM.render(
  <React.StrictMode>
    <EntryFree />
  </React.StrictMode>,
  document.getElementById('root')
);

